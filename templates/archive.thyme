<!DOCTYPE html SYSTEM "http://www.thymeleaf.org/dtd/xhtml1-strict-thymeleaf-4.dtd">

<html xmlns="http://www.w3.org/1999/xhtml" xmlns:th="http://www.thymeleaf.org">
	<head th:replace="header.thyme::head"/>
	<body onload="prettyPrint()">
    <div id="wrap">
    	<div th:replace="menu.thyme::menu"/>
		<div class="container">
			<div class="page-header">
				<h1>Meeting Archive</h1>
			</div>
	
			<div th:each="post : ${posts}" th:remove='tag'>
				<h4>
                    <span th:text='${#dates.format(post.date,"MMMM dd, yyyy")}' th:remove='tag'/> - 
                    <a th:href="${post.uri}" th:text='${post.title}' href="foo.html">Post title</a> 
                    <span style="font-style:italic;font-size:small;">(<span th:text="${post.presenter}">Presenter</span>)</span>
                </h4>
			</div>

		</div>
	</div>
	
	<div th:replace="footer.thyme::footer"></div>
    </body>
</html>